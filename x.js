function bitwiseAND(n1, n2) {
	toString(n1)
	toString(n2)
	for (let i = 0; i < n1.length; index++) {
        if (n1=1 && n2=1) {
            const n1 = 1
        } else {
            const n1=0
        }
        
    }
}

function bitwiseOR(n1, n2) {
	toString(n1)
	toString(n2)
}

function bitwiseXOR(n1, n2) {  
	toString(n2)
	toString(n1)
}